# NetBox Configuration
# Managed by Ansible - DO NOT EDIT MANUALLY

import os

#########################
#                       #
#   Required settings   #
#                       #
#########################

# Database
DATABASE = {
    'NAME': '{{ netbox_db_name }}',
    'USER': '{{ netbox_db_user }}',
    'PASSWORD': '{{ netbox_db_password }}',
    'HOST': '{{ netbox_db_host }}',
    'PORT': {{ netbox_db_port }},
    'CONN_MAX_AGE': {{ netbox_db_conn_max_age }},
}

# Redis
REDIS = {
    'tasks': {
        'HOST': '{{ netbox_redis_host }}',
        'PORT': {{ netbox_redis_port }},
        'PASSWORD': '{{ netbox_redis_password }}',
        'DATABASE': {{ netbox_redis_database_tasks }},
        'SSL': {{ netbox_redis_ssl | lower }},
    },
    'caching': {
        'HOST': '{{ netbox_redis_host }}',
        'PORT': {{ netbox_redis_port }},
        'PASSWORD': '{{ netbox_redis_password }}',
        'DATABASE': {{ netbox_redis_database_caching }},
        'SSL': {{ netbox_redis_ssl | lower }},
    }
}

# Security
SECRET_KEY = '{{ netbox_secret_key }}'

#########################
#                       #
#   Optional settings   #
#                       #
#########################

# Allowed hosts
ALLOWED_HOSTS = {{ netbox_allowed_hosts | to_json }}

# Base URL path
BASE_PATH = ''

# Time zone
TIME_ZONE = '{{ netbox_timezone }}'

# Date/time formatting
DATE_FORMAT = '{{ netbox_date_format }}'
SHORT_DATE_FORMAT = '{{ netbox_date_format }}'
TIME_FORMAT = '{{ netbox_time_format }}'
SHORT_TIME_FORMAT = '{{ netbox_time_format }}'
DATETIME_FORMAT = '{{ netbox_datetime_format }}'
SHORT_DATETIME_FORMAT = '{{ netbox_datetime_format }}'

# Logging
LOG_LEVEL = '{{ netbox_log_level }}'

# Media storage
MEDIA_ROOT = '{{ netbox_media_root }}'

# Reports storage
REPORTS_ROOT = '{{ netbox_reports_root }}'

# Scripts storage
SCRIPTS_ROOT = '{{ netbox_scripts_root }}'

# Enable installed plugins
PLUGINS = []

# Plugins configuration
PLUGINS_CONFIG = {}
